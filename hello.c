#include <cs50.h>
#include <stdio.h>

int main(void)
{
    string answer = get_string("What's your name? ");
    printf("hello, %s\n", answer);
    int counter = 1;
    counter = counter + 1;
    counter += 1;\
    counter++;

}